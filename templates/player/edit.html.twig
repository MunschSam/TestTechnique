{% extends 'base.html.twig' %}

{% block title %}Edit Player{% endblock %}

{% block body %}
    <h1>Edit Player</h1>

    {{ include('player/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('player_show', {'id': player.id}) }}">back to list</a>

    {{ include('player/_delete_form.html.twig') }}
{% endblock %}
